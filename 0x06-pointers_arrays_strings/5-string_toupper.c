#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * *string_toupper - check the code
 * @s: the number of elements to swap
 *
 * Return: nothing.
 */

char *string_toupper(char *s)
{
	char *p = strupr(s);

	return (p);
}
