#include "main.h"
#include <stdio.h>
#include <string.h>

/**
  * _strcmp - strcmp
  * @s1: s one
  * @s2: s two
  * Return: pointer
  */

int _strcmp(char *s1, char *s2)
{
	int p = strcmp(s1, s2);

	return (p);
}
