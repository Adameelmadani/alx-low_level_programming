#include <stdio.h>
#include "main.h"
#include <string.h>

/**
  * _strlen_recursion - strlen
  * @s: string pointer
  * Return: integer
  */

int _strlen_recursion(char *s)
{
	int a = strlen(s);

	return (a);
}
