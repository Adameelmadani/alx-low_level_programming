#include "main.h"
#include <stdio.h>
#include <string.h>

/**
  * _strlen - string length
  * @s: string pointer
  * Return: the pointer
  */

int _strlen(char *s)
{
	int a = strlen(s);

	return (a);
}
